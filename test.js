'use strict';
var assert = require('assert');
var gutil = require('gulp-util');
var exclude = require('./');

describe('exclude', function(){
  it('should exclude files', function(cb){
    //var vStream = gutil.src('**/*.js'); console.log('sfsf');
    //var piped = vStream.pipe(exclude('exclude-dir/**'));
    //vStream.on('end', cb);
    cb();
  });
});
